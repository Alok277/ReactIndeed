const Grocery=()=>{
    return( <h1>This is Grocery App,This contains all Child components in it.
        That is,it generates separate index.js file which help in optimizing the app when application is very large and contains lot of files.
    </h1>)
}
export default Grocery;